<nav class="layout">
    <a href="#">
        <img src="/assets/images/logo/Booki.png" alt="Booki logo"/>
    </a>

    <ul>
        <% const navButtons = data('nav-buttons') %>
        <% for (const button of navButtons) { %>
            <%- include(template('layout/navbar/nav-button'), { button }) %>
        <% } %>
    </ul>
</nav>
